---
- hosts: localhost
  become: yes

  pre_tasks:
    - import_tasks: tasks/set_ubuntu_codename.yml

  tasks:
    - import_tasks: tasks/main.yml

  roles:
    - geerlingguy.pip
    - geerlingguy.docker
    - dotfiles
    - chrome
    - nomachine