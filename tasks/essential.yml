---
- name: Update packages
  apt:
    update_cache: yes
    upgrade: yes

- name: Install essential packages
  package:
    name: "{{ apt_packages }}"
    state: latest

- name: Install packages in snap
  snap:
    name: "{{ snap_packages }}"
    state: present